import 'package:flutter/material.dart';

class LockerRoomPage extends StatelessWidget {
  const LockerRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Locker Rooms'));
  }
}
